#+TITLE: Tests for ob-prolog


* Simple tests

  #+NAME: f
  #+BEGIN_SRC elisp
    '(1 2 3)
  #+END_SRC

  #+RESULTS: f
  | 1 | 2 | 3 |

  #+BEGIN_SRC prolog :var a=f() :var b="asdf" :goal main(A, D) :session *prolog-1* :system swipl
    main(A, B) :- recorded(a, A), recorded(b, C), append([C], A, B).
  #+END_SRC

  #+RESULTS:
  : A = [1, 2, 3],
  : D = [asdf, 1, 2, 3].

  #+BEGIN_SRC prolog :goal other(A) :session *prolog-1*
    other(A) :- A is 3.
  #+END_SRC

  #+RESULTS:
  : A = 3.
